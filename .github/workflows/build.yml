name: Build

on:
  workflow_dispatch:
    inputs:
      tag_name:
        description: 'Tag'
        required: true
        type: string

jobs:
  build:
    name: Build
    permissions:
      id-token: write
      contents: write
      attestations: write
    uses: ./.github/workflows/make.yml
    with:
      tag_name: ${{ inputs.tag_name }}
